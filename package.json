{
  "name": "trim-edge",
  "version": "1.0.0",
  "type": "module",
  "description": "A minimalistic library designed to trim specific strings from the start or end of an input string. If the specified substring exists at the beginning or the end of the original string, it is removed. Otherwise, the original string is returned as is. Great for path manipulation, query parameters, and custom string manipulation needs.",
  "main": "index.js",
  "scripts": {
    "lint": "standard --fix",
    "test": "standard && ava",
    "coverage": "c8 --all --reporter=html --reporter=text npm test"
  },
  "author": "anthony.m.scotti@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "@fast-check/ava": "^1.1.5",
    "ava": "^5.3.1",
    "c8": "^8.0.1",
    "standard": "^17.1.0"
  }
}